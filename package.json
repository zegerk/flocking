{
  "name": "flocking",
  "description": "A real-time friend-and-enemy swarm simulation for WebGL2 and WebAssembly.",
  "private": true,
  "version": "0.2.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "trunk serve",
    "build": "trunk build --release",
    "test:js": "node --test trail-quality.test.mjs versioning.test.mjs perf-harness.test.mjs",
    "test:perf": "node --test perf.test.mjs",
    "perf:bless": "node perf-baseline.mjs --bless",
    "version:check": "node scripts/check-version.mjs",
    "verify": "cargo test && npm run test:js && npm run version:check",
    "verify:perf": "npm run verify && npm run test:perf",
    "release": "node scripts/release.mjs",
    "deploy": "npm run build && wrangler deploy",
    "deploy:env": "set -a && . ./.env && set +a && npm run build && wrangler deploy",
    "cf:dev": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
